<?
    $config = Zend_Registry::get('config');
?>

<h2><?= is_string($config->brand) ? $config->brand : $config->brand->full ?></h2>

<div class="row">
    <div class="col-md-4 col-sm-6">
        <ul class="nav-tree">
            <?= $this->menuItems($config->menu, false, false, false) ?>
        </ul>
    </div>
</div>
